const { KEYWORD } = require("./types");

module.exports = KEYWORDS = {
    DECLARATION: "let",
    CONDITIONAL: "if",
    PROC: "do",
    ALTERNATIVE: "else",
    FUNCTION: "resolve",
    TRUE: "true",
    FALSE: "false"
};